---
name: demo-greet
description: A simple demo skill that greets the user. It reads a JSON object with a 'name' field from stdin and returns a greeting message.
---

# demo-greet

A simple demo skill that greets the user. It reads a JSON object with a 'name' field from stdin and returns a greeting message.
